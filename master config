<cluster>
    <name>wazuh</name> # name of cluster must match with workers
    <node_name>master</node_name> # specific name for this node
    <node_type>master</node_type> # type of node can be 'master' or 'worker'
    <key>dcb855c9f2e8afb29fd93b8c7812f327</key> # key must match with workers (using this command 'openssl rand -hex 16')
    <port>1516</port> # using default port
    <bind_addr>0.0.0.0</bind_addr> # any ip address
    <nodes>
        <node>192.168.1.227</node> # itself ip address
    </nodes>
    <hidden>no</hidden> # set no for visible
    <disabled>no</disabled> # set no for enabled
</cluster>
